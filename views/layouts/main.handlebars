<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Gajraj+One&family=Quicksand:wght@400;500&family=Shantell+Sans:wght@300&display=swap" rel="stylesheet">
  <style>
    body {
      background: whitesmoke;
    }
  </style>
</head>

<body>
  <div class="flex-column justify-space-around" id="blog-pic">
    <header id="headerBlack" class="display-flex justify-space-between align-center p-2">
      <h1>
        <span role="img" aria-label="flex"></span>
        <a href="/" id="gotMilkText">Home</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile" id="profile">Dashboard</a> |
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login" id="login">login</a>
        {{/if}}
      </nav>
    </header>
    <main id="farm-main" class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
